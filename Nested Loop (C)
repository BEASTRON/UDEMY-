#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a, b;

    for(a = 10; a < 80; a++)
    {
        for(b = 2; b <= (a/b); b++)
            if(!(a%b))
            break;
        if(b > (a/b))
            printf("%d is a prime Number!\n", a);
    }

    return 0;
}

